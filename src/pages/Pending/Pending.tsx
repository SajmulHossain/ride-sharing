import Heading from "@/components/common/Heading";

const Pending = () => {
  return (
    <section className="section">
        <Heading heading="Your driver request is pending..." description="Please wait for your driver apporoval. And try againg later!" />
    </section>
  );
};

export default Pending;